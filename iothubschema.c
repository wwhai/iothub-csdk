#include "iothubschema.h"
#include "cJSON.h"
void build_iothub_property_msg(iothub_property_msg msg)
{
    // cJSON *json = cJSON_CreateObject();
    // cJSON *sw1 = cJSON_CreateBool(cJSON_False);
    // cJSON_AddItemToObject(json, "sw1", sw1);
}